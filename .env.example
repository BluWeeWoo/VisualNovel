# Optional custom AI adapter. Leave unset for the fully playable scripted demo.
# The server sends the documented JSON contract; this is NOT a raw provider API URL.
# AI_ADAPTER_URL=https://your-adapter.example/chat
# AI_API_KEY=server-side-secret
# AI_PROVIDER_LABEL=Your provider name
# PORT=4173
